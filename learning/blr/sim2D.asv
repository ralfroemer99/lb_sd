% Define real system
A = [2 1; 3 -1];
B = [0; 1];

% Dimensions
[n,m] = size(B);

% Define control frequency
Ts = 1;

% Compute maximum bound on the uncertainty
alpha = max_disturbance(A,B,sqrt(n)*,Ts);